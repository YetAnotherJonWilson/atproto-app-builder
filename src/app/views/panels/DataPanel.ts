/**
 * Data panel — workspace content for the Data section.
 * Stub: will be implemented in Phase 2.
 */

export function renderDataPanel(): string {
  return `
    <div class="empty-workspace">
      <div class="empty-icon">&#9634;</div>
      <p>Define the Lexicon record types your app reads and writes. Each type has a name,
      NSID, and fields that describe its shape.</p>
      <p><em>Coming soon</em></p>
    </div>
  `;
}
