/**
 * Components panel — workspace content for the Components section.
 * Stub: will be implemented in Phase 4.
 */

export function renderComponentsPanel(): string {
  return `
    <div class="empty-workspace">
      <div class="empty-icon">&#9634;</div>
      <p>Components are reusable UI building blocks. They bind to a data type and get
      assembled into views. Think cards, lists, forms, and detail displays.</p>
      <p><em>Coming soon</em></p>
    </div>
  `;
}
